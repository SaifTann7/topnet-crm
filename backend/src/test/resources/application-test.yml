spring:
  datasource:
    url: jdbc:h2:mem:topnet_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false

topnet:
  jwt:
    secret: test-secret-key-for-unit-tests-minimum-256-bits-long-value!!
    expiration-ms: 3600000
